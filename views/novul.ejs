<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Not vulnerable</title>
</head>
<body>
    <form action="./novul" method="post">
        login
        <input type="text" name="user">
        <br>
        password
        <input type="password" name="pass">
        <button type="submit">submit</button>
    </form>
    <% if (!prijava) { %>
    <% } else if (prijava && auth && users.length > 0) { %>
        <% for (user of users) {%>
            <% for (attr of attrs) {%> 
                <p><%= attr %>: <%= user[attr] %></p>
            <% } %> 
            <br>
        <% } %>
    <% } else { %>
        <p>Korisnik nije pronađen. Korisničko ime ili lozinka nisu ispravni.</p>
        <% if (timeout) {%>
            <p>Pokušajte ponovno za 5 sekundi.</p>
        <% } %> 
    <% } %>
</body>
</html>